import { useState as o, useEffect as r } from "react";
const l = () => {
  const t = typeof window < "u" && "DeviceOrientationEvent" in window, [a, i] = o({
    alpha: null,
    beta: null,
    gamma: null,
    absolute: !1
  });
  return r(() => {
    if (!t) return;
    const n = (e) => i({
      alpha: e.alpha,
      beta: e.beta,
      gamma: e.gamma,
      absolute: e.absolute
    });
    return window.addEventListener("deviceorientation", n), () => {
      window.removeEventListener("deviceorientation", n);
    };
  }, []), {
    supported: t,
    value: a
  };
};
export {
  l as useDeviceOrientation
};
//# sourceMappingURL=useDeviceOrientation.mjs.map
