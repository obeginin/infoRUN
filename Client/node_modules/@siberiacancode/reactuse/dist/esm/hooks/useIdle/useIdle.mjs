import { useState as a, useEffect as l } from "react";
import { throttle as f } from "../../utils/helpers/throttle.mjs";
const h = [
  "mousemove",
  "mousedown",
  "keydown",
  "touchstart",
  "wheel",
  "resize"
], v = 6e4, L = (t = v, { initialValue: u = !1, events: o = h } = {}) => {
  const [d, s] = a(u), [m, E] = a(Date.now());
  return l(() => {
    let e;
    const r = () => s(!0), n = f(() => {
      s(!1), E(Date.now()), clearTimeout(e), e = setTimeout(r, t);
    }, 500), c = () => {
      document.hidden || n();
    };
    return e = setTimeout(r, t), o.forEach((i) => window.addEventListener(i, n)), document.addEventListener("visibilitychange", c), () => {
      o.forEach((i) => window.addEventListener(i, n)), document.removeEventListener("visibilitychange", c), clearTimeout(e);
    };
  }, [t, o]), { idle: d, lastActive: m };
};
export {
  L as useIdle
};
//# sourceMappingURL=useIdle.mjs.map
