import { useRef as u, useState as c } from "react";
import { useUnmount as m } from "../useUnmount/useUnmount.mjs";
const f = (t) => {
  const e = u(0), [n, r] = c(t), a = (o) => {
    cancelAnimationFrame(e.current), e.current = requestAnimationFrame(() => r(o));
  };
  return m(() => cancelAnimationFrame(e.current)), [n, a];
};
export {
  f as useRafState
};
//# sourceMappingURL=useRafState.mjs.map
