import { useState as i, useEffect as u } from "react";
const p = () => {
  const [s, m] = i(0);
  return u(() => {
    let e = 0, n = performance.now(), t;
    const r = () => {
      e += 1;
      const o = performance.now(), a = o - n;
      if (a >= 1e3) {
        const c = Math.round(e * 1e3 / a);
        m(c), e = 0, n = o;
      }
      t = requestAnimationFrame(r);
    };
    return t = requestAnimationFrame(r), () => {
      cancelAnimationFrame(t);
    };
  }, []), s;
};
export {
  p as useFps
};
//# sourceMappingURL=useFps.mjs.map
