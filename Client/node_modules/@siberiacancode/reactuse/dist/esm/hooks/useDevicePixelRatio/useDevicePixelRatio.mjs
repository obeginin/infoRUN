import { useState as d, useEffect as a } from "react";
const r = () => {
  const e = typeof window < "u" && typeof window.matchMedia == "function" && typeof window.devicePixelRatio == "number", [i, n] = d(window.devicePixelRatio ?? 1);
  return a(() => {
    if (!e) return;
    const t = () => n(window.devicePixelRatio), o = window.matchMedia(`(resolution: ${window.devicePixelRatio}dppx)`);
    return o.addEventListener("change", t), () => {
      o.removeEventListener("change", t);
    };
  }, [i]), { supported: e, ratio: i };
};
export {
  r as useDevicePixelRatio
};
//# sourceMappingURL=useDevicePixelRatio.mjs.map
