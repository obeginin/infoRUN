import { useState as n } from "react";
import { flushSync as c } from "react-dom";
const i = (s = []) => {
  const [e, r] = n(s);
  return {
    add: (t) => r((o) => [...o, t]),
    remove: () => {
      let t;
      return c(() => {
        r(([o, ...u]) => (t = o, u));
      }), t;
    },
    clear: () => r([]),
    first: e[0],
    last: e[e.length - 1],
    size: e.length,
    queue: e
  };
};
export {
  i as useQueue
};
//# sourceMappingURL=useQueue.mjs.map
