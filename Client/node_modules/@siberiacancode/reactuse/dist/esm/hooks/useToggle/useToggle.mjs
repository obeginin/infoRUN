import { useReducer as t } from "react";
const f = (r = [!1, !0]) => {
  const [[c], u] = t((e, o) => {
    const s = typeof o == "function" ? o(e[0]) : o, n = Math.abs(e.indexOf(s));
    return e.slice(n).concat(e.slice(0, n));
  }, r);
  return [c, u];
};
export {
  f as useToggle
};
//# sourceMappingURL=useToggle.mjs.map
